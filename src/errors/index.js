const { InvalidCommandError } = require("./InvalidCommandError");
const { MalformedCommandError } = require("./MalformedCommandError");
const { InvalidMethodError } = require("./InvalidMethodError");

module.exports = {
    InvalidCommandError,
    MalformedCommandError,
    InvalidMethodError
};
